from setuptools import setup

setup(
    name='jsonparser',
    version='0.0.1',
    description = 'Process json and convert it into dataframes',
    py_modules=["jsonparser"],
    package_dir={'':'src'},
    
)